sonarqube {
    properties {
        property "sonar.projectKey", rootProject.name
        property "sonar.projectName", rootProject.name
        property "sonar.projectBaseDir", project.rootDir
        property "sonar.inclusions", "main/src/main/java/**"

        property "sonar.coverage.jacoco.xmlReportPaths", "${jacoco.reportsDir}/test/jacocoTestReport.xml"
        property "sonar.jacoco.reportPath", null
        property "sonar.jacoco.reportPaths", null
    }
}